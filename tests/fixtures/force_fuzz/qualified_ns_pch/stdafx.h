// SPDX-License-Identifier: Apache-2.0
// A precompiled-header-style include that does NOT declare the target — mirrors
// the MFC StdAfx.h case where the auto-included header lacks the namespaced free
// function, so the harness must emit its own forward declaration.
#pragma once
